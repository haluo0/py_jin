services:
  - type: web
    name: py-jin-inspection
    env: python
    region: oregon          # 可选：oregon, frankfurt 等
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9       # 推荐指定 Python 版本