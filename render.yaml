services:
  - type: web
    name: inspection-system
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -b 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.7"          # 👈 关键：降级到 Python 3.12
      - key: ADMIN_PASSWORD
        sync: false 
      - fromDatabase:
          name: inspection-db
          property: connectionString
        key: DATABASE_URL 

databases:
  - name: inspection-db
    plan: free 